"use client";

import { motion } from "framer-motion";
import React, { useState } from "react";

import { Swiper, SwiperSlide } from "swiper/react";
import "swiper/css";

import { BsArrowUpRight, BsGithub } from "react-icons/bs";

import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip";

import Link from "next/link";
import Image from "next/image";
import WorkSliderBtns from "@/components/WorkSliderBtns";

const projects = [
  {
    num: "01",
    category: "Robotic",
    title: "project 1",
    description:
      "Designed and developed an intelligent logistics robot that scans terrain to autonomously generate optimal routes for efficient and precise item delivery.",
    stack: [{ name: "RaspberryPi" }, { name: "LEGO Mindstorm EV3" }, { name: "Python" }],
    image: "/assets/work/Project 1.jpg",
  },
  {
    num: "02",
    category: "Hardware",
    title: "project 2",
    description:
      "Developed a radar-inspired short-range obstacle detection system designed to provide reliable mobility assistance for visually impaired individuals.",
    stack: [{ name: "Arduino" }, { name: "Python" }],
    image: "/assets/work/Project 2.jpg",
  },
  {
    num: "03",
    category: "Hardware",
    title: "project 3",
    description:
      "Developed an ultrasonic levitation device that enables multiple plastic foam spheres to float steadily in mid-air upon activation.",
    stack: [{ name: "Altium Designer" }, { name: "Surface Mounting" }],
    image: "/assets/work/Project 3.jpg",
  },
  {
    num: "04",
    category: "Hardware",
    title: "project 4",
    description:
      "Designed and developed a smart wristband featuring a clock display, heart rate monitoring, temperature sensing, step tracking, and fall detection alerts. This wearable device integrates multiple health and safety functionalities to promote user well-being and provide real-time feedback on daily activities and potential emergencies.",
    stack: [{ name: "Altium Designer" }, { name: "Surface Mounting" },{ name: "STM 32" },{ name: "C" }],
    image: "/assets/work/Project 4.jpg",
  },
];

const Work = () => {
  const [project, setProject] = useState(projects[0]);

  const handleSlideChange = (swiper) => {
    // get current slide index
    const currentIndex = swiper.activeIndex;
    // update project state based on current slide index
    setProject(projects[currentIndex]);
  };

  return (
    <motion.section
      initial={{ opacity: 0 }}
      animate={{
        opacity: 1,
        transition: { delay: 2.4, duration: 0.4, ease: "easeIn" },
      }}
      className="min-h-[80vh] flex flex-col justify-center py-12 xl:px-0"
    >
      <div className="container mx-auto">
        <div className="flex flex-col xl:flex-row xl:gap-[30px]">
          <div className="w-full xl:w-[50%] xl:h-[460px] flex flex-col xl:justify-between order-2 xl:order-none">
            <div className="flex flex-col gap-[30px] h-[50%]">
              {/* outline num */}
              <div className="text-8xl leading-none font-extrabold text-transparent text-outline-hover">
                {project.num}
              </div>
              {/* project category */}
              <h2 className="text-[42px] font-bold leading-none text-white group-hover:text-orange-400 transition-all duration-500 capitalize">
                {project.category} project
              </h2>
              {/* project description */}
              <p className="text-white/60">{project.description}</p>
              {/* stack */}
              <ul className="flex gap-4">
                {project.stack.map((item, index) => {
                  return (
                    <li key={index} className="text-xl text-orange-400">
                      {item.name}
                      {/* remove the last comma */}
                      {index !== project.stack.length - 1 && ","}
                    </li>
                  );
                })}
              </ul>
              {/* border */}
              <div className="border border-white/20"></div>
              {/* buttons */}
              <div className="flex items-center gap-4">
              </div>
            </div>
          </div>
          <div className="w-full xl:w-[50%]">
            <Swiper
              spaceBetween={30}
              slidesPerView={1}
              className="xl:h-[520px] mb-12"
              onSlideChange={handleSlideChange}
            >
              {projects.map((project, index) => {
                return (
                  <SwiperSlide key={index} className="w-full">
                    <div className="h-[460px] relative group flex justify-center items-center bg-pink-50/20">
                      {/* overlay */}
                      <div className="absolute top-0 bottom-0 w-full h-full bg-black/10 z-10"></div>
                      {/* image */}
                      <div className="relative w-full h-full">
                        <Image
                          src={project.image}
                          fill
                          className="object-cover"
                          alt=""
                        />
                      </div>
                    </div>
                  </SwiperSlide>
                );
              })}
              {/* slider buttons */}
              <WorkSliderBtns
                containerStyles="flex gap-2 absolute right-0 bottom-[calc(50%_-_22px)] xl:bottom-0 z-20 w-full justify-between xl:w-max xl:justify-none"
                btnStyles="bg-orange-400 hover:bg-orange-400-hover text-primary text-[22px] w-[44px] h-[44px] flex justify-center items-center transition-all"
              />
            </Swiper>
          </div>
        </div>
      </div>
    </motion.section>
  );
};

export default Work;
